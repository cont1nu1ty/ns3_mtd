# Hard-coded framework constraints (must not be user-configurable).

# Minimum tick interval to prevent high-frequency cross-language calls.
MIN_TICK_INTERVAL_MS = 100

# Maximum number of events processed per tick.
MAX_EVENTS_PER_TICK = 1000
